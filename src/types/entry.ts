type Entry = {
  bigTags: string | null;
  definition: string | null;
  deinflectors: string | null;
  link: string | null;
  reading: string;
  tags: string | null;
  term: string;
};

export default Entry;
